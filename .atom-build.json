{
  "cmd": "scons debug=1 && gnome-terminal -e \"bash -c './test; read'\"",
  "sh": true,
  "errorMatch": "(?<file>[^: \\n]+):(?<line>\\d+):(?<col>\\d+): ",
  "targets": {
    "Android": {
      "cmd": "cd android && ./build.sh"
    }
  }
}
