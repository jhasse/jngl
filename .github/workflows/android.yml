name: Android

on:
  pull_request:
  push:

jobs:
  build:
    runs-on: [ubuntu-latest]
    container:
      image: registry.gitlab.com/jhasse/android-ndk:22-1-7171670
    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: make -C android
